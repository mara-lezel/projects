package ecosystem.enums;

/**
 * TerrainType --- determines the properties of a
 * tile object (e.g. plants can grow in SOIL,
 * but not on ROCK surfaces or WATER).
 */
public enum TerrainType { SOIL, ROCK, WATER }
